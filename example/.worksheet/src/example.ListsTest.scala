package example

object ListsTest {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(79); 
  println("Welcome to the Scala worksheet");$skip(33); val res$0 = 
  
  Lists.max(List(1, 2, 3, 4));System.out.println("""res0: Int = """ + $show(res$0))}
}
